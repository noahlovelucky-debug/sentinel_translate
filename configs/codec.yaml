paths:
  output: /data/code/sentinel_translat/v3.2/checkpoints_v32_recovery
  reports: /data/code/sentinel_translat/v3.2/reports_v32_recovery

train:
  stage: codec
  max_steps: 20000
  num_workers: 4
  learning_rate: 0.0001
  warmup_steps: 1000

validation:
  full_steps: []
