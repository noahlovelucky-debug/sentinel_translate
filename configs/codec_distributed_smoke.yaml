paths:
  output: /data/code/sentinel_translat/v3.2/checkpoints_distributed_smoke
  reports: /data/code/sentinel_translat/v3.2/reports_distributed_smoke

train:
  stage: codec
  max_steps: 2
  batch_size: 4
  num_workers: 1
  warmup_steps: 1
  log_every: 1
  optical_dists_weight: 0.0
  validate_every: 1000
  save_every: 2

validation:
  enabled: false
